### README

- find repo object in `bitbucket_repos_remaining.json`
- cut + paste in `bitbucket_repos.json`
- run `bitbucket_to_github_migration.sh`
- move object from `bitbucket_repos.json` to `bitbucket_repos_migrated.json`
- commit
- prevent pushing to bitbucket repo
